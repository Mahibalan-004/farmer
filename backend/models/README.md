# Backend Models Directory

This directory is reserved for data models (User, Farmer, Buyer, Product, Order, etc.).
Models will interface with MySQL database schema once the database module is configured.
